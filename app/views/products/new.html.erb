<% if user_signed_in? %>
<%= render 'layouts/navbar1' %>
<% else %>
<%= render 'layouts/navbar' %>
<%end%>

<h1>New Product</h1>

<%= render 'form', product: @product %>

<%= link_to 'Back', products_path %>
