<div>

    <h3>Product</h3>

        <p><%= @product.name %> from <%= @product.user.email %></p>
        <p><%= image_tag (@product.img_url), style: 'width: 200px; height: 170px' %></p>
        <p><%= @product.description %></p>

</div>
